#include <stdio.h>
int main()
{
  printf("\narethmatic oprations");
  int a=27;
  int b=17;
  printf("addion is=%d",a + b );
  
  printf("subtraction is=%d",a - b );
  
  printf("multiplication is=%d",a * b );
  
  printf("divison is=%d",a / b );
  
  printf("reminder is=%d",a % b );
  return 0;
}
